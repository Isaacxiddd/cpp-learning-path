#include <iostream>
#include "../../../../lib/funciones/files.hpp"
#include "../../../../lib/funciones/strings.hpp"
#include "../../../../lib/funciones/tokens.hpp"
#include "../../../../lib/funciones/Coll.hpp"
#include "principal.hpp"

using std::cin;
using std::cout;
using std::endl;
using std::getline;
using std::string;
using std::to_string;

// ─── Enunciado ─────────────────────────────────────────────────────────────────
// Archivos:
//   ACTAS.dat         → novedades (se recorre una sola vez)
//   INFRACCIONES.dat  → consulta (se sube a memoria)
//
// Se pide:
//   1) Por cada tipo de infraccion, cantidad de actas por dia del mes
//      (dia = fecha % 100).
//   2) Por cada patente, total deuda, total descuento y neto a pagar
//      (descuento dtoPromo sobre penalidad si paga antes de diasPromo).

int main()
{
	// TODO: resolvé acá (estructuras de trabajo, subir consultas,
	// procesamiento de novedades y muestra).

	std::cin.get();
	return 0;
}
