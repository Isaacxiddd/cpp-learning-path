#include <iostream>
#include "../../../../lib/funciones/files.hpp"
#include "../../../../lib/funciones/strings.hpp"
#include "../../../../lib/funciones/tokens.hpp"
#include "../../../../lib/funciones/Coll.hpp"
#include "principal.hpp"

using std::cin;
using std::cout;
using std::endl;
using std::getline;
using std::string;
using std::to_string;

// ─── Enunciado ─────────────────────────────────────────────────────────────────
// Archivos:
//   FUNCIONES.dat → novedades (idFuncion = idObra*100 + secuencia)
//   RESERVAS.dat  → novedades
//   OBRAS.dat     → consulta (se sube a memoria)
//   TEATROS.dat   → consulta (se sube a memoria)
//
// Capacidad de un sector = capacidad/sectores del teatro; solo se aceptan
// reservas completas. Se pide: por cada obra y por cada funcion, la cantidad
// de reservas excluidas por falta de capacidad.

int main()
{
	// TODO: resolvé acá (estructuras de trabajo, subir consultas,
	// procesamiento de novedades y muestra).

	std::cin.get();
	return 0;
}
