#include <iostream>
#include "../../../../lib/funciones/files.hpp"
#include "../../../../lib/funciones/strings.hpp"
#include "../../../../lib/funciones/tokens.hpp"
#include "../../../../lib/funciones/Coll.hpp"
#include "principal.hpp"

using std::cin;
using std::cout;
using std::endl;
using std::getline;
using std::string;
using std::to_string;

// ─── Enunciado ─────────────────────────────────────────────────────────────────
// Archivos:
//   RESULTADOS.dat → novedades (se recorre una sola vez)
//   EQUIPOS.dat    → consulta (se sube a memoria) y actualizacion
//
// codRes < 0 gana idEq1, > 0 gana idEq2, = 0 empate. Ganador suma 3 puntos,
// empate 1 a cada uno. Se pide:
//   1) Tabla de posiciones actualizada.
//   2) Por cada estadio, partidos jugados y empatados.
//   3) Actualizar los puntos en EQUIPOS.dat.

int main()
{
	// TODO: resolvé acá (estructuras de trabajo, subir consultas,
	// procesamiento de novedades y muestra).

	std::cin.get();
	return 0;
}
