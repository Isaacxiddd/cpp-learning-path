#include <iostream>
#include "../../../../lib/funciones/files.hpp"
#include "../../../../lib/funciones/strings.hpp"
#include "../../../../lib/funciones/tokens.hpp"
#include "../../../../lib/funciones/Coll.hpp"
#include "principal.hpp"

using std::cin;
using std::cout;
using std::endl;
using std::getline;
using std::string;
using std::to_string;

// ─── Enunciado ─────────────────────────────────────────────────────────────────
// Archivo:
//   MUESTRAS.dat → novedades, sin orden ('E' enciende, 'C' cambia de canal,
//                  'A' apaga)
//
// Generar MINAMIN.dat (ordenado por canal y min) describiendo, por cada canal,
// cuantos monitores lo sintonizaron en cada minuto del dia (min de 0 a 1439).
// Un canal se sintoniza desde el 'C' (o 'E') hasta el proximo 'C' (o 'A'),
// siempre por al menos 1 minuto.

int main()
{
	// TODO: resolvé acá (estructuras de trabajo, subir consultas,
	// procesamiento de novedades y muestra).

	std::cin.get();
	return 0;
}
