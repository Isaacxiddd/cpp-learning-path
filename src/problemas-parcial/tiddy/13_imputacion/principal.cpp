#include <iostream>
#include "../../../../lib/funciones/files.hpp"
#include "../../../../lib/funciones/strings.hpp"
#include "../../../../lib/funciones/tokens.hpp"
#include "../../../../lib/funciones/Coll.hpp"
#include "principal.hpp"

using std::cin;
using std::cout;
using std::endl;
using std::getline;
using std::string;
using std::to_string;

// ─── Enunciado ─────────────────────────────────────────────────────────────────
// Archivos:
//   HORAS.dat      → novedades, ordenado por idEmpleado + idProy
//   PROYECTOS.dat  → consulta (se sube a memoria)
//   EMPLEADOS.dat  → consulta (se sube a memoria)
//
// Corte de control (corte por empleado, luego por proyecto). Se pide:
//   1) Por proyecto, total de horas imputadas y cantidad de empleados.
//   2) Por empleado, total de horas imputadas y cantidad de proyectos.

int main()
{
	// TODO: resolvé acá (estructuras de trabajo, subir consultas,
	// procesamiento de novedades y muestra).

	std::cin.get();
	return 0;
}
