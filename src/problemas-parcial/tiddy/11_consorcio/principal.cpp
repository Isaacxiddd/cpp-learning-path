#include <iostream>
#include "../../../../lib/funciones/files.hpp"
#include "../../../../lib/funciones/strings.hpp"
#include "../../../../lib/funciones/tokens.hpp"
#include "../../../../lib/funciones/Coll.hpp"
#include "principal.hpp"

using std::cin;
using std::cout;
using std::endl;
using std::getline;
using std::string;
using std::to_string;

// ─── Enunciado ─────────────────────────────────────────────────────────────────
// Archivos:
//   GASTOS.dat        → novedades (variante A: ordenado por idCons)
//   CONSORCIOS.dat    → consulta (se sube a memoria)
//   DISTRIBUCION.dat  → consulta, sin orden, acotada (TAD Distrib)
//
// Se pide:
//   1) Por consorcio, listado de departamentos ordenados decrecientemente por
//      mtsPorcentuales (proporcion del piso) con el total a pagar, mas la
//      distribucion de los gastos por categoria.
//   2) TAD Distrib (sobre DISTRIBUCION.dat) para reemplazar mtsPorcentuales.

int main()
{
	// TODO: resolvé acá (estructuras de trabajo, subir consultas,
	// procesamiento de novedades y muestra).

	std::cin.get();
	return 0;
}
