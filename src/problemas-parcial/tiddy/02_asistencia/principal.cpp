#include <iostream>
#include "../../../../lib/funciones/files.hpp"
#include "../../../../lib/funciones/strings.hpp"
#include "../../../../lib/funciones/tokens.hpp"
#include "../../../../lib/funciones/Coll.hpp"
#include "principal.hpp"

using std::cin;
using std::cout;
using std::endl;
using std::getline;
using std::string;
using std::to_string;

// ─── Enunciado ─────────────────────────────────────────────────────────────────
// Archivos:
//   MOVILES.dat    → consulta (se sube a memoria)
//   ABONADOS.dat   → consulta (se sube a memoria)
//
// Programa interactivo: 1 = llama abonado, 2 = llama movil, 3 = finalizar.
// Asignar movil/abonado cuando ambos esperan; notificarAsignacion(celular,
// nroCaso, nombre). Al finalizar, por cada movil, los casos con tiempo de
// atencion por debajo del promedio de la zona.

int main()
{
	// TODO: resolvé acá (estructuras de trabajo, subir consultas,
	// procesamiento de novedades y muestra).

	std::cin.get();
	return 0;
}
