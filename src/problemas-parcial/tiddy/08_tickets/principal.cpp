#include <iostream>
#include "../../../../lib/funciones/files.hpp"
#include "../../../../lib/funciones/strings.hpp"
#include "../../../../lib/funciones/tokens.hpp"
#include "../../../../lib/funciones/Coll.hpp"
#include "principal.hpp"

using std::cin;
using std::cout;
using std::endl;
using std::getline;
using std::string;
using std::to_string;

// ─── Enunciado ─────────────────────────────────────────────────────────────────
// Archivos:
//   PRODUCTOS.dat  → consulta (se sube a memoria)
//   RUBROS.dat     → consulta (se sube a memoria)
//
// Por cada venta se ingresa idCli y pares {idProd, cant}; idProd=0 cierra la
// compra, idCli=0 cierra la operatoria. Se pide:
//   1) Ticket por venta: items agrupados y ordenados alfabeticamente por
//      descripcion, con precio c/dto del rubro y ahorro por rubro.
//   2) Los 10 productos mas demandados (decreciente por cantidad).

int main()
{
	// TODO: resolvé acá (estructuras de trabajo, subir consultas,
	// procesamiento de novedades y muestra).

	std::cin.get();
	return 0;
}
