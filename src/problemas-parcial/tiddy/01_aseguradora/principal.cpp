#include <iostream>
#include "../../../../lib/funciones/files.hpp"
#include "../../../../lib/funciones/strings.hpp"
#include "../../../../lib/funciones/tokens.hpp"
#include "../../../../lib/funciones/Coll.hpp"
#include "principal.hpp"

using std::cin;
using std::cout;
using std::endl;
using std::getline;
using std::string;
using std::to_string;

// ─── Enunciado ─────────────────────────────────────────────────────────────────
// Archivos:
//   EMPRESAS.dat    → consulta (se sube a memoria)
//   ACCIDENTES.dat  → novedades (se recorre una sola vez)
//
// Se pide:
//   1) Por cada empresa, el porcentaje de reajuste de su tarifa
//      (dado por porcentaje(cantTrabAsegurados, cantAccidentes)).
//   2) Por cada empresa, listado de trabajadores accidentados con el total
//      de dias de licencia ausentes (ordenado por idEmpresa).

int main()
{
	// TODO: resolvé acá (estructuras de trabajo, subir consultas,
	// procesamiento de novedades y muestra).

	std::cin.get();
	return 0;
}
